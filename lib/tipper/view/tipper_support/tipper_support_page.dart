import 'package:flutter/material.dart';

class TipperSupporPage extends StatelessWidget {
  const TipperSupporPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
